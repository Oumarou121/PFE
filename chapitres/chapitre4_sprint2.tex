%=================================================
% CHAPITRE 4 : SPRINT 2
%=================================================

\chapter{Sprint 4 : Moteur de données RH (Dossiers et Mouvements)}

\section{Introduction}

Une fois les fondations techniques posées, le véritable défi métier de SIRH-Doc a commencé : comment gérer des dossiers personnels complexes sans coder un formulaire différent pour chaque type de donnée ? Un enseignant permanent n'a pas les mêmes informations qu'un vacataire, et pourtant, le système doit pouvoir les traiter avec la même fluidité.

Ce sprint a été l'occasion de mettre en place le \og cœur battant \fg{} des données RH de l'établissement, en utilisant une approche dynamique pour les formulaires et les listes.

\section{Objectifs et Backlog du sprint}

L'objectif était de permettre aux gestionnaires RH de sortir du \og tout papier \fg{} ou des fichiers Excel éparpillés pour centraliser tout le parcours d'un collaborateur.

\begin{itemize}
    \item \textbf{Dossiers Individuels Dynamiques} : Création des fiches pour permanents et vacataires avec leurs données d'état civil et professionnelles.
    \item \textbf{Numérisation des Pièces} : Gestion des diplômes et justificatifs directement rattachés au profil.
    \item \textbf{Suivi des Positions et Mouvements} : Tracer si un agent est en activité, en congé, en mission ou en fin de carrière.
    \item \textbf{Historique des Engagements} : Suivre les contrats successifs, particulièrement pour les vacataires, année par année.
\end{itemize}

\section{Choix techniques et décisions de conception}

\subsection{L'approche par "Vues de Tables" configurables}

Plutôt que de figer les champs dans le code Angular, j'ai implémenté un système de \texttt{TableViewConfig}. C'est une décision qui a tout changé. L'idée est simple : on définit en base de données quels champs d'une table SQL sont visibles, lesquels sont modifiables et lesquels servent de filtres.
Cette flexibilité m'a permis de créer l'interface de gestion des diplômes en quelques minutes une fois que le moteur de vue était prêt. C'est ce qui fait de SIRH-Doc une plateforme paramétrable et non un simple logiciel RH figé.

\subsection{Le défi de la traçabilité des parcours}

Tracer les mouvements (changements de poste ou de grade) a été complexe. J'ai dû concevoir une logique où chaque \og mouvement \fg{} est une ligne chronologique. Au départ, je voulais juste mettre à jour le statut dans la fiche principale, mais j'aurais perdu tout l'historique. J'ai donc opté pour une table de mouvements dédiée, liée à l'année universitaire choisie au Sprint 1.

\section{Analyse et Conception}

\subsection{Diagramme de cas d'utilisation}

L'administrateur RH devient ici l'acteur central. Il peut naviguer dans les différents modules (Personnel, Positions, Contrats) que j'ai structurés de façon modulaire.

\diagramplaceholder{sprint2_cas_utilisation}{Cas d'utilisation du Sprint 2}{Gestion opérationnelle des données RH}

\subsection{Diagramme de classes (Entités persistantes)}

Le modèle de données s'enrichit ici avec les entités de base du SIRH. On remarque la séparation entre l'agent (\texttt{Personnel}), ses diplômes et le journal de ses positions administratives.

\diagramplaceholder{sprint2_classes}{Diagramme de classes - Sprint 2}{Modèle de données du personnel et des parcours}

\subsection{Diagramme de séquence : Création d'un mouvement}

Ce diagramme illustre comment le système valide un changement de position (par exemple un départ en congé) en s'assurant que la donnée est bien enregistrée dans le contexte de l'année universitaire active.

\diagramplaceholder{sprint2_sequences}{Séquence de mise à jour d'un parcours}{Enregistrement d'un mouvement administratif}

\section{Réalisation technique}

\subsection{Formulaires génériques et Lookups}

Pour faciliter la saisie, j'ai développé des composants Angular capables de lire la configuration backend et de générer les listes déroulantes (\textit{lookups}) automatiquement. Si une table fait référence à un département, le formulaire va chercher tout seul la liste des départements disponibles en base.

\subsection{Gestion documentaire des pièces jointes}

Même si le gros de l'édition vient au Sprint 3, j'ai intégré ici la possibilité d'associer des fichiers (PDF, scans) aux dossiers. J'ai choisi de stocker les chemins et les métadonnées en base pour garder une base SQL légère et performante.

\section{Conclusion du sprint}

À la fin de cette étape, SIRH-Doc était devenu un véritable outil de gestion. On pouvait créer un dossier, ajouter des diplômes et voir l'évolution de la carrière d'un agent. Il ne restait plus qu'à transformer ces données brutes en documents officiels élégants, ce qui allait être l'objet du sprint suivant.
